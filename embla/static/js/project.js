/* Project-specific JavaScript for the Django-rendered pages.
   Referenced by embla/templates/base.html; must exist even when empty —
   whitenoise's manifest storage raises a 500 on any {% static %} reference
   missing from the collectstatic manifest. */
